import 'package:flutter/material.dart';



const kgoldenColor = LinearGradient(colors: [Color(0xF4FFE998),Color(0xF0573700)],begin: Alignment.topLeft,end: Alignment(0.1, 0.0));
const kblackColor = LinearGradient(colors: [Color(0xFF000000),Color(0xF0414141)],begin: Alignment.centerRight);

const kblack = Color(0xFF01050A);
const kgold = Color(0xFFDFA622);

const kDefaultPadding = 20.0;

const kDefaultShadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12, // Black color with 12% opacity
);
